//what this config does:
// removes network cap to allow max network information beint sent between you and the server
// change cmdrate/updaterate to maximum values (servers will still force it down but this just makes it so you're always using the max value oln the server)
// change interp for better lag comp

rate "786432"
cl_cmdrate "128"
cl_updaterate "128"
cl_interp_ratio "1"
cl_interp "0"
cl_lagcompensation "1"
cl_predict "1"
cl_predictweapons "1"
